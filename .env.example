# Backend
REDIS_URL=redis://localhost:6379/0
STORAGE_PATH=./storage
DATABASE_URL=sqlite+aiosqlite:///./ocrbooks.db
TORCH_DEVICE=cpu

# Frontend (dev)
VITE_API_URL=http://localhost:8000

# AI provider — configurable desde la UI (Settings ⚙ en el sidebar)
# Se guarda en storage/ai_config.json, no hace falta definir aquí.
